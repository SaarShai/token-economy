# Reference RTK config. Optional extension; not vendored runtime.

[tee]
mode = "failures"

[hooks]
exclude_commands = ["git status", "ls", "pwd"]

[filters]
level = "aggressive"
preserve_errors = true

