Please could you help me understand how this React component actually re-renders? I've basically been trying to figure it out for hours and I'm just really stuck. The component in question is located at ./src/components/UserProfile.tsx and it seems to re-render every single time the parent component updates, even when the props passed in haven't actually changed in any meaningful way.

The code in question looks something like this:

```tsx
function UserProfile({ user, onUpdate }: Props) {
  const config = { showAvatar: true, theme: 'dark' };
  return <Card config={config} onClick={() => onUpdate(user.id)} />;
}
```

It would really be great if you could explain what's happening here. I think it might have something to do with object references or closures, but honestly I'm not entirely sure. Any help you could provide would be very much appreciated, and please feel free to go into as much detail as you think is necessary.

Of course, the performance impact is actually significant when there are hundreds of these components on the page at the same time. We've measured render times of around 250ms which is way too slow for our use case. The target would be under 50ms for the full list.
