# Token Economy Adapter

Read `start.md` first. Keep startup context lean.

- Default style: Caveman Ultra.
- Run `./te doctor`; load only `L0_rules.md` and `L1_index.md`.
- Use `./te wiki search|timeline|fetch` for memory retrieval.
- Use `./te delegate classify|plan` before spawning subagents.
- At 20% context used, run `./te context checkpoint` and continue from a fresh session packet when possible.
- Document durable facts only after verified execution.

